package com.example.demo.entity;

public enum NotificationSeverity {
    INFO,      // Blue - General info
    SUCCESS,   // Green - Success actions
    WARNING,   // Orange - Warnings (low stock, deletions)
    DANGER     // Red - Critical (errors, failures)
}